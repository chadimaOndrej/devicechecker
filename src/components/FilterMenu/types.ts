export interface MultipleSelectTypes {
  value: string[]
}

export interface PhoneTypes {
  id: string,
  code?: string,
  image?: string,
  model?: string,
  os?: string,
  osVersion?: string,
  vendor?: string,
}
